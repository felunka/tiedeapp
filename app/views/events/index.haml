.card.mt-3
  %h5.card-header.d-flex.justify-content-between.align-items-center
    Familientage
    = button_to 'Familientag erstellen', new_event_path, class: 'btn btn-primary', method: :get
  .table-responsive
    %table.table
      %tr
        %th
          Name
        %th
          Ort
        %th
          Start
        %th
          Ende
        %th
          Anmeldeschluss
        %th
      - @events.each do |event|
        %tr
          %td
            = event.name
          %td
            = event.location
          %td
            = event.event_start
          %td
            = event.event_end
          %td
            = event.deadline_signup
          %td
            .d-flex
              = button_to edit_event_path(event), class: 'btn btn-primary', method: :get do
                %i.fas.fa-pen
              = button_to event_path(event), class: 'btn btn-danger', method: :delete, data: { confirm: "Sind Sie sicher?" } do
                %i.fa.fa-trash-alt
