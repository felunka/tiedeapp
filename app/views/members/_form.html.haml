.card.mt-3
  %h5.card-header.d-flex.justify-content-between.align-items-center
    = t('model.member._name')
    .d-flex.model-buttons
      - if @member.id.present?
        = button_to t('helpers.submit.delete'), member_path(@member), class: 'btn btn-danger', method: :delete
      = button_to t('helpers.submit.cancel'), members_path, class: 'btn btn-secondary', method: :get

  .card-body
    = simple_form_for @member do |f|
      .row
        .col
          = f.input :first_name
        .col
          = f.input :last_name
      = f.input :street
      .row
        .col-2
          = f.input :zip
        .col
          = f.input :city
      = f.input :country
      = f.input :member_type, collection: Member.member_types.symbolize_keys.keys, include_blank: false, include_hidden: false, label: t('model.member.member_type')
      = f.input :date_of_birth, as: :date, html5: true
      = f.input :email
      = f.input :phone
      = f.submit class: 'btn btn-primary mt-2'